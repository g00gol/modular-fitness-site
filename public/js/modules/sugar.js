// client side 
const sugar_form = document.getElementById('sugarTrackerForm');

if (sugar_form) 
{
    sugar_form.addEventListener('submit', (e) => 

    {
        e.preventDefault();
        let sugar = document.getElementById('sugarInput').value;
        let fast = document.getElementById('fastInput').value;
        
    })
}
